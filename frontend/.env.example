# ============================================================
#  SAFE-HAVEN — Frontend Environment Variables
#  Copy this file to .env and fill in your values.
# ============================================================

# Deployed Soroban contract ID (from `stellar contract deploy`)
VITE_CONTRACT_ID=CAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAABSC4

# Stellar network passphrase
# Testnet:  "Test SDF Network ; September 2015"
# Mainnet:  "Public Global Stellar Network ; September 2015"
VITE_NETWORK_PASSPHRASE="Test SDF Network ; September 2015"

# Soroban RPC endpoint
# Testnet:  https://soroban-testnet.stellar.org
# Mainnet:  https://soroban.stellar.org
VITE_RPC_URL=https://soroban-testnet.stellar.org

# Horizon endpoint (for account queries)
VITE_HORIZON_URL=https://horizon-testnet.stellar.org

# Stellar Expert explorer base URL
VITE_EXPLORER_URL=https://stellar.expert/explorer/testnet

# (Optional) Source account used for read-only simulations when no wallet is connected.
# Defaults to the contract ID if unset. Set this to a funded account on your target network
# to avoid synthetic-account fallback. On mainnet, set it to any existing funded account.
# VITE_SIMULATION_ACCOUNT=

# ============================================================
#  Price Oracle Configuration (CoinGecko API)
# ============================================================
#
# Prices are fetched automatically every 60 seconds and displayed in USD.
# No additional configuration required — uses CoinGecko's free public API.
#
# For higher rate limits (>50 calls/min), sign up at https://www.coingecko.com/api
# To add more tokens, update TOKEN_TO_COINGECKO_ID in src/lib/priceService.ts
#
